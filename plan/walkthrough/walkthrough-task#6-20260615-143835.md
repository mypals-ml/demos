# Task #6 Walkthrough - 2026-06-15 14:38:35 JST

Added project completion dashboard demo with scheduling metrics, assumptions, warnings, and recalculating controls; verified content, JS syntax, browser render, recalculation, and local console.

## Task
Demo a dashboard for a project.

## Implementation
- Added `/Users/wanghui/coding/prj-demos/demos/demos/project-dashboard/index.html`.
- Built a standalone static dashboard for GitHub Project-style task data.
- Added forecast controls for start date, worker capacity, and scenario.
- Added summary metrics for:
  - Estimated Completion Date.
  - Total Remaining Effort.
  - Missing Data Warnings.
  - Active Workers.
- Added a project task table showing status, estimate, assignee/default worker, dependency, remaining effort, and scheduled dates.
- Implemented deterministic browser-side scheduling rules:
  - Done tasks contribute `0` remaining effort.
  - In progress and in review tasks contribute `50%` remaining effort.
  - Todo and draft tasks contribute `100%` remaining effort.
  - Missing estimates emit a warning and default to `1` day.
  - Missing assignees emit a warning and schedule against `Default worker`.
  - Capacity defaults to `6` hours per day.
  - Work is scheduled Monday-Friday.
  - Dependencies must finish before successors start.
- Added assumptions and warning panels so the forecast rules are explicit.
- Added a compact capacity timeline for open work.
- Updated `/Users/wanghui/coding/prj-demos/demos/index.html` to link the new demo and increase the count to `7 items`.
- Updated `/Users/wanghui/coding/prj-demos/demos/implementation_plan.md` for task #6.

## Verification
- Ran a content check confirming the dashboard includes:
  - `Estimated Completion Date`.
  - `Total Remaining Effort`.
  - `Assumptions Used`.
  - `Missing Data Warnings`.
  - `Dependencies finish first`.
  - The `/demos/project-dashboard/` index link.
  - The root shelf count of `7 items`.
- Ran an inline JavaScript syntax check with `new Function(...)`; one inline script validated.
- Ran `git diff --check`; no whitespace errors were reported.
- Served the static site locally at `http://localhost:4173`.
- Opened `/demos/project-dashboard/` in the browser.
- Verified initial render showed:
  - Completion date `Jun 16, 2026`.
  - Remaining effort `1.0d`.
  - `5` warning rows.
  - `5` project task rows.
  - Assumptions and dependency-aware timeline.
- Changed capacity to `3` hours/day and scenario to `Blocked review`.
- Verified recalculation showed:
  - Capacity assumption `Capacity = 3h/day`.
  - Completion date `Jun 18, 2026`.
  - Remaining effort `4.0d`.
  - Warning count `7`.
  - `5` task rows.
  - `2` timeline rows.
- Checked local-page console error logs; no `localhost:4173` errors were reported.
